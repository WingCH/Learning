import 'package:flutter/material.dart';

class Case1SubPage extends StatelessWidget {
  const Case1SubPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Case1 Sub Page'),
      ),
      body: Center(
        child: const Text('Case1 Sub Page'),
      ),
    );
  }
}
