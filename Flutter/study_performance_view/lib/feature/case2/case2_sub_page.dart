import 'package:flutter/material.dart';

class Case2SubPage extends StatelessWidget {
  const Case2SubPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Case2SubPage'),
      ),
    );
  }
}
